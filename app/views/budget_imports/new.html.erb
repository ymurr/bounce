


<div class="row">
  <div class="col-md-6 col-md-offset-3">

<h1>Budget Import</h1>

<p>A CSV or Excel file can be used to import records. The first row should be the column name. The following columns are allowed.</p>

<ul>
  <% Budget.columns.each do |column| %>
    <% if column.name.in? ["account", "decription", "annual_budget", "year_to_date", "prior_year_to_date", "percent_budget_used"] %>
      <li>
        <strong><%= column.name %></strong> -
        <%= column.type.to_s.titleize %> type
      </li>
    <% end %>
  <% end %>
</ul>

<p>If an <strong>id</strong> is supplied it will update the matching record instead of creating a new one.</p>

<%= form_for @budget_import do |f| %>
  <% if @budget_import.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@budget_import.errors.count, "error") %> prohibited this import from completing:</h2>
      <ul>
      <% @budget_import.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.file_field :file %>
  </div>
        <br/>

      <div>  <%= f.submit "Import", class: "btn btn-primary" %> </div>
<% end %>


  </div>
</div>
